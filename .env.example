# ── Server only (BFF) — never prefix with VITE_ ──
FLYAI_API_KEY=your-flyai-api-key
BFF_PORT=3001

# ── Client (Vite) — only VITE_* is exposed to the browser ──
VITE_APP_NAME=杭蓉机票
VITE_DEV_PORT=5173
